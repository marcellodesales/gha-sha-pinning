name: Call gha-lint
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    paths:
      - '.github/**'
jobs:
  gha-sha-pinning:
    name: 📌 gha-sha-pinning
    permissions:
      contents: write
      pull-requests: write
    uses: Finatext/workflows-public/.github/workflows/gha-lint.yml@main
    secrets: inherit
